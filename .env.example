# Endpoint público do Worker; a chave da OpenAI permanece apenas no secret OPENAI_API_KEY do Cloudflare.
# Sem esta variável, o site usa respostas canônicas locais para desenvolvimento e indisponibilidade temporária.
VITE_CHARACTER_CHAT_API_URL=https://SEU-WORKER.workers.dev/api/character-chat
